<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart</title>
    {{>css}}
    <script src="https://cdn.jsdelivr.net/npm/axios@1.6.7/dist/axios.min.js"></script>
</head>
<body>
    {{>navbar}}
    <div class="main-container">
        <h1>Welcome To Cart</h1>
        <div>
            Total Price:{{totalPrice}}
        </div>
        <div class="cart-list-container">
            <ul class="cart-list">
                {{#each cart as |c|}}
                <li class="cart-item">
                    <div class="cart-product-image">
                        <img src="{{c.id.imageUrl}}" alt="Product Image">
                    </div>
                    <div class="cart-product-details">
                        <p>Name: {{c.id.name}} </p>
                        <p>Price: {{c.id.price}}</p>
                        {{id._id}}
                        String:{{idString}}
                        <p class="quantity-box">Quantity: 
                            <button onclick="increase({{idString}})" class="quantity-button increase">
                                {{!-- <a class="quantity" href="shop/cart/increase">  --}}
                                +
                                {{!-- </a> --}}
                            </button > 
                                {{c.quantity}} 
                            <button onclick="decrease()" class="quantity-button decrease">
                                {{!-- <a class="quantity" href="/shop/cart/decrease"> --}}
                                -
                                {{!-- </a> --}}
                            </button> 
                        </p>
                    </div>
                </li>
                {{/each}}
                <div class="buy-btn-box">
                    <button class="cartBuy">
                    <a href="/shop/cart/buy" >Buy Now</a>
                </button>
                </div>
            </ul>

        </div>
    </div>

    <script src="/js/script.js"></script>
</body>
</html>