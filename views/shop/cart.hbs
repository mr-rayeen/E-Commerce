<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart</title>
    {{>css}}
</head>
<body>
    {{>navbar}}
    <div class="main-container">
        <h1>Welcome To Cart</h1>
        <div>
            {{!-- Total Price:{{totalPrice}} --}}
        </div>
        <div class="cart-list-container">
            <ul class="cart-list">
            {{#each cart as |c|}}
            <li class="cart-item">
                <div class="cart-product-image">
                    <img src="{{c.id.imageUrl}}" alt="Product Image">
                </div>
                <p>Name: {{c.id.name}} </p>
                <p>Price: {{c.id.price}}</p>
                <p>Quantity: <button><a class="quantity" href="shop/cart/increse"> + </a></button> {{c.quantity}} <button><a class="quantity" href="/shop/cart/decrease"> - </a></button> </p>
            </li>
            {{/each}}
        </ul>
        </div>
    </div>
</body>
</html>